export default function ShoppingList(){
    return (
        <>
            {/*
                1. given a specific user / week, retrieve all meals
                2. for each meal, retrieve all ingredients
                3. all all ingredients together
                4. nicely format by group (carb)
                5. displays
            */}
        </>
    );
}