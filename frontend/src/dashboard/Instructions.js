export default function Instructions(){
    return (
        <>
            {/*
                1. compile a list of all the meals, group by meal key?
                
            */}
        
        </>
    );
}